name: Publish MiroTalk mobile profile

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: rgaliull
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and publish immutable mobile profile
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.upstream
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/mirotalk-sfu-public:2.4.43-upstream1
            ghcr.io/${{ github.repository_owner }}/mirotalk-sfu-public:sha-${{ github.sha }}

      - name: Make the container package public
        env:
          GH_TOKEN: ${{ secrets.GHCR_TOKEN }}
        run: >-
          gh api --method PATCH
          /user/packages/container/mirotalk-sfu-public
          -f visibility=public
